import React from 'react';

const HeroSection = () => {
	return (
		<div>
			<h1>Welcome to the Dashboard!</h1>
			<p>This is a protected section that only logged-in users can see.</p>
		</div>
	);
};

export default HeroSection;
